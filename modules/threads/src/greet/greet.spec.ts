import { greet } from "./greet";
// import {  } from 'underscore';

describe("Greet", () => {
  it("should ", () => {
    expect(2).toBe(2);
    // expect(greet()).toBe('Hello from users!');
  });
});

// "@types/jest": "29.0.3",
// "jest": "^29.0.3",
// "ts-jest": "^27.0.3"
